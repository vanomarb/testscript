{
    "name": "testscript",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm --prefix src run dev\" \"npm --prefix server run dev\"",
        "build": "npm --prefix src run build",
        "start:prod": "npm run build && npm --prefix server run start"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
